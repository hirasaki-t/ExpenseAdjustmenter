# DbContextクラスを継承したクラスが存在するプロジェクト名
$project = "DbMigration"

# 起動時に呼ばれるプロジェクト名
$startupProject = "DbMigration"

# DbContextクラスを継承したマイグレーション対象のクラス
$context = "SqlServerDbContext"

# cmdフォルダから見たソリューションフォルダが存在するパスへの相対パス
$solutionFolder = "../src/DbMigration"
